FROM python:3.8

ADD algorithm /app/

# RUN apt-get update && \
#     apt-get install --no-install-recommends -y \
#     python3-tk \
#     tk-dev \
#     libagg-dev

# RUN pip install wheel
# RUN pip install -r /app/requirements.txt

